extends layout

block content
  h1= title
  
  ul
    each order in book_list
      li 
        a(href=order.url) #{order.dateTime}
        |  (#{order.client.name})

    else
      li There are no books.
